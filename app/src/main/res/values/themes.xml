<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LoginUi" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/purple_200</item>
        <item name="colorControlNormal">@color/grey_dark</item>
        <item name="colorControlActivated">@color/purple_500</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="circular">
        <item name="cornerSize">50%</item>
        <item name="background">@color/white</item>
    </style>

    <style name="toobartext" parent="TextAppearance.MaterialComponents.Button">
        <item name="android:textSize">16dp</item>
    </style>
    <style name="Quiz.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="background">@color/white</item>
        <item name="theme">@style/Theme.LoginUi</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="titleTextAppearance">@style/toobartext</item>
    </style>
</resources>